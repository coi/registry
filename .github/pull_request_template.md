## Library Submission

### Summary
- Library name:
- Repository URL:
- First release version:

### Checklist
- [ ] Added a new file under `libraries/` (for example: `libraries/my-lib.json`)
- [ ] `schema-version` is set
- [ ] `repository` points to the correct GitHub repo
- [ ] `compiler-drop.min` is set (optimistic minimum)
- [ ] `compiler-drop.tested-on` is set (verified compiler drop)
- [ ] `releases` contains at least one release entry
- [ ] `createdAt` and `releasedAt` use `YYYY-MM-DD`
- [ ] Library repository license is MIT
- [ ] Ran `python3 scripts/validate_registry.py --offline` locally

### Notes (optional)
- Any extra context for reviewers
